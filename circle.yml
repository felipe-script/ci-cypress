version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
     test:
    #  - cypress/run
      steps: 
        - checkout
      - restore_cache:
          keys:
            - cache-{{ checksum "package.json" }}
         - run:
          name: Install dependencies
          command: npm ci   
        - run: npm run